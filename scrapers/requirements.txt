# Core dependencies for scraper pipeline
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
feedparser>=6.0,<7.0
supabase>=2.0,<3.0

# Name normalization
unidecode>=1.3,<2.0

# LLM enrichment
anthropic>=0.40,<1.0
instructor>=1.0,<2.0
pydantic>=2.0,<3.0

# Data quality / dedup (defer heavy deps until needed)
# splink[duckdb]>=4.0
# crawlee[beautifulsoup]>=0.4

# Dev / testing
pytest>=8.0,<9.0
python-dotenv>=1.0,<2.0
